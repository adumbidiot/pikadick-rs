[package]
name = "open-graph"
version = "0.0.0"
authors = [ "adumbidiot <nathaniel.daniel23@outlook.com>" ]
edition = "2021"
license = "MIT OR Apache-2.0"

[[bin]]
name = "open-graph"
path = "src/main.rs"
required-features = [ "cli" ]

[dependencies]
scraper = { version = "0.25.0", default-features = false }
thiserror = "2.0.18"
url = "2.5.8"

# Optional
anyhow = { version = "1.0.102", optional = true }
argh = { version = "0.1.19", optional = true }
reqwest = { version = "0.13.3", default-features = false, optional = true }
tokio = { version = "1.52.3", optional = true }

[features]
default = []
client = [
    "reqwest",
    "tokio",
    "tokio/rt",
]

client-default-tls = [
    "client",
    "reqwest/default-tls"
]

client-native-tls = [
    "client",
    "reqwest/native-tls"
]

client-rustls = [
    "client",
    "reqwest/rustls"
]

cli = [ 
    "anyhow",
    "argh",
    "client",
    "tokio/rt-multi-thread",
    "tokio/fs",
]
