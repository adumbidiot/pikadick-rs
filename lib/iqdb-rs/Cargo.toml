[package]
name = "iqdb"
version = "0.0.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[[bin]]
name = "iqdb"
path = "src/main.rs"
required-features = [ "cli" ]

[dependencies]
lazy_static = "1.5.0"
reqwest = { version = "0.13.3", default-features = false, features = [ "multipart", "stream" ] }
scraper = { version = "0.25.0", default-features = false }
thiserror = "2.0.18"
tokio = { version = "1.52.3", features = [ "fs", "rt" ] }
tokio-util = { version = "0.7.18", features = ["codec"] }
url = "2.5.8"

# Optional
anyhow = { version = "1.0.102", optional = true }
argh = { version = "0.1.19", optional = true }

[dev-dependencies]
tokio = { version = "1.52.3", features = [ "macros" ] }

[features]
default = [ "reqwest/default-tls" ]

native-tls = [ "reqwest/native-tls" ]
rustls = [ "reqwest/rustls" ]

cli = [
    "anyhow",
    "argh",
    "tokio/rt-multi-thread",
]
